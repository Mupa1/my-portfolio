import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  root: {
    paddingBottom: '100px',
  },
  techStack: {
    fontSize: '12px',
    margin: '5px 5px 5px',
    borderRadius: '8px',
  },
}));

export default useStyles;
